---
title: "Tools"
description: "The tool portfolio, and the difference between a tool and a tool config."
---

A **tool** is a capability module shipped with the framework. A **tool config** is an
instance of one, holding your credentials and parameters. Agents reference tool config
ids, never tool names.

```
GET /api/tools                     list available tools
GET /api/tools/{tool_name}/params  parameters a config must provide
POST /api/tools_config             create a configured instance
```

The portfolio ships **32 modules**, together exposing over a hundred callable tools.

<Frame caption="The tool box: 107 tools across 32 modules, plus MCP servers and skills.">
  <img src="/images/toolbox.jpg" alt="Tool box listing available tools, tool categories, MCP servers and skills" />
</Frame>

## Portfolio

### Google Workspace

| Module | Covers |
| --- | --- |
| `google_auth` | The OAuth handshake shared by the Google modules |
| `google_drive` | List, search, read, download files |
| `google_gmail` | List, search, read, send email |
| `google_calendar` | List, search, create events |
| `google_sheets` | Spreadsheet info, read and write cells |
| `google_docs` | Create, read, append |

### Microsoft 365

| Module | Covers |
| --- | --- |
| `microsoft_auth` | The OAuth handshake shared by the Microsoft modules |
| `outlook_mail` | List, search, read, send, folders, attachments |
| `onedrive_core` | Shared OneDrive plumbing |
| `onedrive_read` | List, search, read, download, shared files |
| `onedrive_write` | Upload, create folders, delete |
| `teams` | Chats, messages, replies, search, group chats, members |

### Data and databases

| Module | Covers |
| --- | --- |
| `database` | Generic queries and SQL execution |
| `database_mcp` | Database access over MCP |
| `text_to_sql` | Natural language to SQL, schema introspection |
| `db_to_rag` | Index query results into RAG |
| `data_handler` | Filtering, aggregation, transformation |
| `business_intelligence` | Datasets, charts and dashboards |

### Knowledge and memory

| Module | Covers |
| --- | --- |
| `rag` | Create, list, search and delete knowledge bases |
| `memory` | Save and search user memory and tool usage patterns |

### Business systems

| Module | Covers |
| --- | --- |
| `odoo` | Search, create and update CRM/ERP records on an Odoo instance |
| `marketing` | HubSpot integration, sales leads |

### Content and media

| Module | Covers |
| --- | --- |
| `artifacts` | Files an agent produces during a run |
| `audio` | Audio handling |
| `image_generation` | Image generation |
| `visualization` | Chart generation, CSV to chart export |

### Utilities

| Module | Covers |
| --- | --- |
| `basic` | Read files, load data, convert images, bearer tokens |
| `api_call` | Generic API calls |
| `s3_tools` | Read and search S3 files |
| `web_search_mcp` | Web search over MCP |
| `integration_status` | Whether a given integration is connected and healthy |
| `thaink2` | thaink2 platform utilities |

## Skills

Beyond tools, the framework ships **skills** — packaged procedures an agent can follow,
each one a `SKILL.md` with its references:

`dashboard-builder` · `data-visualization` · `email-campaign` · `error-recovery` ·
`onedrive-navigation` · `rag-search` · `report-generation` · `text-to-sql`

## MCP servers

Tools can also come from an MCP server declared per workspace. `GET /api/mcp_configs`
lists the configured servers; the modules suffixed `_mcp` above reach their capabilities
through that channel.

<Note>
  A module being present does not mean it is usable: most need a tool config carrying
  credentials, and the Workspace modules need the matching
  [integration](/guides/integrations) connected first.
</Note>
